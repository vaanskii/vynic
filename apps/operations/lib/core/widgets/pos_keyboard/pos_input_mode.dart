/// TEXT edits Unicode text; NUMBER/DECIMAL constrain digits; PIN is masked and
/// is never disabled by the optional operational keyboard preference.
enum PosInputMode { text, number, decimal, pin }
